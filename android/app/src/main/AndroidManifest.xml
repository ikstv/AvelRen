<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Потрібен для registerNetworkCallback: щойно зв'язок відновлюється,
         екран оновлюється одразу, не чекаючи наступного 60-секундного циклу.
         Normal-permission, runtime-запиту не потребує. У merged-маніфесті він
         і так з'являвся транзитивно від WorkManager — оголошуємо явно, щоб не
         залежати від чужої залежності. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- З Android 13 дозвіл на сповіщення треба просити явно. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AvelRenApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AvelRen">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AvelRen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".notify.AvelRenMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Приймає натиск «ОК» просто зі шторки, без відкриття застосунку. -->
        <receiver
            android:name=".notify.AckReceiver"
            android:exported="false" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/alert_channel_id" />
    </application>

</manifest>
